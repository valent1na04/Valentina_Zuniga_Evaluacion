package cl.ubiobio.tareita;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;

@DisplayName("Tareita Application Tests")
class TareitaApplicationTests {

	@Test
	@DisplayName("Application context should be loadable")
	void contextLoads() {
		// This test verifies that the application can start
		// For integration tests with full Spring context, use @SpringBootTest
		// For unit tests, we don't need the full context
	}

}
